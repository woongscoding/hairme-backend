# Lambda deployment requirements
# Based on working 11/27 deployment + hairstyle synthesis

# FastAPI 및 웹 프레임워크
# fastapi 0.115+: google-genai가 요구하는 httpx>=0.28과 호환되는 starlette 버전 필요
fastapi==0.115.14
uvicorn[standard]==0.27.0
python-multipart==0.0.20

# 데이터베이스
sqlalchemy==2.0.25
pymysql==1.1.1
cryptography==43.0.3
boto3==1.35.0

# Redis 캐싱
redis==5.0.1

# Google Gemini AI
google-generativeai>=0.8.0
google-genai>=1.52.0

# 이미지 처리
pillow==10.4.0
opencv-python-headless==4.9.0.80
numpy==1.26.3
# mediapipe는 Dockerfile.lambda에서 --no-deps로 설치한다.
# 선언된 의존성(jax/jaxlib/opencv-contrib-python/matplotlib)은 FaceMesh 런타임에
# matplotlib·cv2만 실제로 import되며(2026-09-10 이미지 내부 확인), jax/jaxlib(307MB)와
# opencv-contrib(cv2 중복 180MB)는 콜드스타트 시간만 늘린다. 아래는 mediapipe의 실제 런타임 의존성.
absl-py>=1.4.0
attrs>=19.1.0
flatbuffers>=2.0
protobuf>=4.25.3,<5
matplotlib==3.8.2

# MLOps
pyyaml==6.0.1

# Circuit Breaker
pybreaker==1.0.1

# 회원 인증 (Kakao 로그인 + JWT)
PyJWT==2.10.1
httpx>=0.27.0  # 카카오 API 호출

# Google Play 인앱결제 영수증 검증
google-auth==2.45.0  # 서비스 계정 인증 (무거운 google-api-python-client 대신 REST 직접 호출)

# Lambda Handler
mangum==0.17.0

# 기타
pydantic==2.10.0
pydantic-settings==2.1.0
tqdm==4.67.1
slowapi==0.1.9
psutil==5.9.6

# Monitoring & Observability
sentry-sdk[fastapi]==2.19.2
