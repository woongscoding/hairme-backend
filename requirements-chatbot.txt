# LangGraph 챗봇 전용 의존성 (개인 학습용 - 실서비스 Lambda 이미지에 포함하지 않음)
#
# services/chatbot/ 은 독립 실험 프로젝트로, 메인 앱(main.py)에 통합되지 않는다.
# 로컬에서 챗봇을 실행할 때만 설치:
#   pip install -r requirements.txt -r requirements-chatbot.txt

langgraph==1.1.4
langchain-core==1.2.23
langchain-community==0.4.1
langchain-openai==1.1.12
langchain-text-splitters==1.1.1
chromadb==1.3.7  # RAG 벡터 저장소 (onnxruntime 등 무거운 의존성 포함 - 메인 이미지 제외 사유)
tavily-python==0.7.23  # web_search 노드
